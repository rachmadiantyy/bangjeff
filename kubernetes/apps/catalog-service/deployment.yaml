apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: bangjeff-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      imagePullSecrets:
        - name: do-regcred
      containers:
        - name: catalog-service
          image: registry.digitalocean.com/bangjeff/catalog-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: bangjeff-env-service
            - secretRef:
                name: root-app-bangjeff--secrets
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: bangjeff-dev
spec:
  selector:
    app: catalog-service
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
