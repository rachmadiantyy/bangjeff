apiVersion: apps/v1
kind: Deployment
metadata:
  name: supplier-service
  namespace: bangjeff-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: supplier-service
  template:
    metadata:
      labels:
        app: supplier-service
    spec:
      imagePullSecrets:
        - name: do-regcred
      containers:
        - name: supplier-service
          image: registry.digitalocean.com/bangjeff/supplier-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: bangjeff-env-service
            - secretRef:
                name: root-app-bangjeff--secrets
---
apiVersion: v1
kind: Service
metadata:
  name: supplier-service
  namespace: bangjeff-dev
spec:
  selector:
    app: supplier-service
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
