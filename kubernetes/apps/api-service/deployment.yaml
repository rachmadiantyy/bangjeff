apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service
  namespace: bangjeff-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-service
  template:
    metadata:
      labels:
        app: api-service
    spec:
      imagePullSecrets:
        - name: do-regcred
      containers:
        - name: api-service
          image: registry.digitalocean.com/bangjeff/api-service:latest
          ports:
            - containerPort: 9090
          envFrom:
            - configMapRef:
                name: bangjeff-env-service
            - secretRef:
                name: root-app-bangjeff--secrets
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: bangjeff-dev
spec:
  selector:
    app: api-service
  ports:
    - port: 80
      targetPort: 9090
  type: ClusterIP
