apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: bangjeff-dev
  labels:
    app: admin-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
        role: web
    spec:
      containers:
        - name: admin-service
          image: registry.digitalocean.com/bangjeff/admin-service:latest
          ports:
            - containerPort: 9090
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: bangjeff-dev
spec:
  selector:
    app: admin-service
  ports:
    - port: 80
      targetPort: 9090
